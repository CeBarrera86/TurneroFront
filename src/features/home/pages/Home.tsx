import React from 'react';
import { Grid, Typography } from '@mui/material';

const Home = () => {
  return (
    <Grid container spacing={3}>
      <Typography />
    </Grid>
  );
};

export default Home;
